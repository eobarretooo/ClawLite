# Demo GIF Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Generate `docs/demo.gif` — janela macOS-style animada mostrando `clawlite run "o que você pode fazer?"` com efeito de digitação e resposta em streaming.

**Architecture:** `scripts/make_demo_gif.py` — script standalone que usa Playwright para renderizar um HTML terminal-style, captura PNG por estado (16 frames), e usa Pillow para montar o GIF. Sem servidor externo, sem API key, sem dependências novas.

**Tech Stack:** `playwright` (chromium já instalado em `/root/.cache/ms-playwright`), `Pillow>=12.0` (já instalado), Python stdlib.

**Spec:** `docs/superpowers/specs/2026-03-15-demo-gif-design.md`

**Sequência de frames (16 total):**
1–5: digitação do prompt (80ms cada)
6–7: cursor piscando após digitação (400ms cada)
8–9: spinner `⠸ / ⠴ pensando...` (800ms cada)
10: intro da resposta (600ms)
11–15: bullets progressivos (520ms cada)
16: footer + pausa final (3200ms)

---

## Chunk 1: Terminal HTML template

### Task 1: Criar template HTML do terminal

**Files:**
- Create: `scripts/terminal_template.py` — `build_html()`, `TermLine`, `_render_prompt_partial()`
- Test: `tests/scripts/test_terminal_template.py`

- [ ] **Step 1: Criar diretório de testes e `scripts/__init__.py`**

```bash
mkdir -p /root/build/ClawLite/tests/scripts
touch /root/build/ClawLite/tests/scripts/__init__.py
touch /root/build/ClawLite/scripts/__init__.py
```

- [ ] **Step 2: Escrever o teste que vai falhar**

```python
# tests/scripts/test_terminal_template.py
from __future__ import annotations


def test_template_returns_string():
    from scripts.terminal_template import build_html
    html = build_html(lines=[])
    assert isinstance(html, str)
    assert "<!DOCTYPE" in html


def test_template_has_traffic_lights():
    from scripts.terminal_template import build_html
    html = build_html(lines=[])
    assert "#ff5f57" in html  # vermelho
    assert "#febc2e" in html  # amarelo
    assert "#28c840" in html  # verde


def test_template_renders_lines():
    from scripts.terminal_template import build_html, TermLine
    html = build_html(lines=[
        TermLine(text="hello world", color=None),
        TermLine(text="colored", color="#89b4fa"),
    ])
    assert "hello world" in html
    assert "colored" in html
    assert "#89b4fa" in html


def test_template_dimensions_720():
    from scripts.terminal_template import build_html
    html = build_html(lines=[])
    assert "720" in html


def test_template_partial_prompt_shows_typing():
    """Prompt parcial deve aparecer com cursor e cor correta."""
    from scripts.terminal_template import build_html
    # Digitando só "clawlite"
    html = build_html(lines=[], partial_prompt="clawlite", show_cursor=True)
    assert "clawlite" in html
    assert "cursor" in html  # classe CSS do cursor
    # Digitando argumento
    html2 = build_html(lines=[], partial_prompt='clawlite run "o que', show_cursor=True)
    assert "o que" in html2
```

- [ ] **Step 3: Rodar para confirmar falha**

```bash
cd /root/build/ClawLite && python3 -m pytest tests/scripts/test_terminal_template.py -v 2>&1 | tail -10
```
Expected: `ModuleNotFoundError: No module named 'scripts.terminal_template'`

- [ ] **Step 4: Criar `scripts/terminal_template.py`**

```python
# scripts/terminal_template.py
"""HTML template da janela terminal macOS-style para o demo GIF.

Tema Catppuccin Mocha. Suporta efeito de digitação via partial_prompt.
"""
from __future__ import annotations
from dataclasses import dataclass


@dataclass
class TermLine:
    text: str
    color: str | None = None   # cor CSS ou None para cor padrão (#cdd6f4)
    bold: bool = False
    indent: int = 0            # colunas de indentação extras


# Cores Catppuccin Mocha
PROMPT_GREEN     = "#a6e3a1"
PROMPT_CMD_COLOR = "#89b4fa"
PROMPT_ARG_COLOR = "#89dceb"
_PROMPT_PREFIX   = "❯"
_FULL_CMD        = "clawlite"
_FULL_RUN        = " run "
_FULL_ARG        = '"o que você pode fazer?"'


def build_html(
    *,
    lines: list[TermLine],
    width: int = 720,
    height: int = 400,
    show_prompt: bool = True,
    partial_prompt: str | None = None,
    show_cursor: bool = False,
    spinner: str | None = None,
) -> str:
    """Retorna HTML completo pronto para screenshot via Playwright.

    Args:
        partial_prompt: quando definido, mostra apenas esse trecho do comando
                        (usado para efeito de digitação). Ignora show_prompt=False.
    """
    lines_html  = _render_lines(lines)
    spinner_html = f'<div class="spinner">{spinner}</div>' if spinner else ""

    if partial_prompt is not None:
        prompt_html = _render_prompt_partial(partial_prompt, show_cursor)
    elif show_prompt:
        cursor_html = '<span class="cursor">█</span>' if show_cursor else ""
        prompt_html = (
            f'<div class="line prompt-line">'
            f'<span style="color:{PROMPT_GREEN}">{_PROMPT_PREFIX}</span> '
            f'<span style="color:{PROMPT_CMD_COLOR}">{_FULL_CMD}</span>'
            f'{_FULL_RUN}'
            f'<span style="color:{PROMPT_ARG_COLOR}">{_FULL_ARG}</span>'
            f'{cursor_html}'
            f'</div>'
        )
    else:
        prompt_html = ""

    return f"""<!DOCTYPE html>
<html>
<head>
<meta charset="utf-8">
<style>
  * {{ margin: 0; padding: 0; box-sizing: border-box; }}
  body {{
    width: {width}px; height: {height}px;
    background: #181825;
    display: flex; align-items: center; justify-content: center;
    font-family: 'Courier New', Courier, monospace;
  }}
  .window {{
    width: {width - 40}px;
    background: #1e1e2e;
    border-radius: 10px; overflow: hidden;
    box-shadow: 0 20px 60px rgba(0,0,0,0.6);
  }}
  .titlebar {{
    background: #2a2a3e; padding: 10px 14px;
    display: flex; align-items: center; gap: 7px;
    border-bottom: 1px solid #313244;
  }}
  .dot {{ width: 12px; height: 12px; border-radius: 50%; }}
  .dot-red    {{ background: #ff5f57; }}
  .dot-yellow {{ background: #febc2e; }}
  .dot-green  {{ background: #28c840; }}
  .title {{ margin-left: auto; font-size: 12px; color: #585b70; letter-spacing: 0.3px; }}
  .content {{ padding: 18px 20px; font-size: 13px; line-height: 1.8; color: #cdd6f4; min-height: 200px; }}
  .line {{ white-space: pre-wrap; }}
  .prompt-line {{ margin-bottom: 8px; }}
  .spinner {{ color: #6c7086; margin: 4px 0 8px; }}
  .cursor {{
    display: inline-block; background: #cdd6f4; color: #1e1e2e;
    margin-left: 2px; width: 8px;
  }}
</style>
</head>
<body>
  <div class="window">
    <div class="titlebar">
      <div class="dot dot-red"></div>
      <div class="dot dot-yellow"></div>
      <div class="dot dot-green"></div>
      <span class="title">clawlite — terminal</span>
    </div>
    <div class="content">
      {prompt_html}
      {spinner_html}
      {lines_html}
    </div>
  </div>
</body>
</html>"""


def _render_prompt_partial(partial: str, show_cursor: bool) -> str:
    """Renderiza o prompt sendo digitado com colorização progressiva."""
    cursor_html = '<span class="cursor">█</span>' if show_cursor else ""
    cmd_len = len(_FULL_CMD)
    run_len = len(_FULL_CMD) + len(_FULL_RUN)

    if len(partial) <= cmd_len:
        body = f'<span style="color:{PROMPT_CMD_COLOR}">{partial}</span>'
    elif len(partial) <= run_len:
        rest = partial[cmd_len:]
        body = (f'<span style="color:{PROMPT_CMD_COLOR}">{_FULL_CMD}</span>{rest}')
    else:
        arg_part = partial[run_len:]
        body = (
            f'<span style="color:{PROMPT_CMD_COLOR}">{_FULL_CMD}</span>'
            f'{_FULL_RUN}'
            f'<span style="color:{PROMPT_ARG_COLOR}">{arg_part}</span>'
        )

    return (
        f'<div class="line prompt-line">'
        f'<span style="color:{PROMPT_GREEN}">{_PROMPT_PREFIX}</span> '
        f'{body}{cursor_html}'
        f'</div>'
    )


def _render_lines(lines: list[TermLine]) -> str:
    parts = []
    for line in lines:
        indent = "&nbsp;" * (line.indent * 2)
        text = line.text.replace("&", "&amp;").replace("<", "&lt;").replace(">", "&gt;")
        weight = "font-weight:bold;" if line.bold else ""
        if line.color:
            parts.append(
                f'<div class="line">{indent}'
                f'<span style="color:{line.color};{weight}">{text}</span></div>'
            )
        else:
            style = f' style="{weight}"' if weight else ""
            parts.append(f'<div class="line"{style}>{indent}{text}</div>')
    return "\n".join(parts)
```

- [ ] **Step 5: Rodar testes**

```bash
cd /root/build/ClawLite && python3 -m pytest tests/scripts/test_terminal_template.py -v 2>&1 | tail -15
```
Expected: `5 passed`

- [ ] **Step 6: Commit**

```bash
cd /root/build/ClawLite
git add scripts/__init__.py scripts/terminal_template.py tests/scripts/
git commit -m "feat(scripts): add terminal HTML template with typing animation support (macOS-style, Catppuccin Mocha)"
```

---

## Chunk 2: Captura de frames com Playwright

### Task 2: Capturar screenshots PNG por estado

**Files:**
- Create: `scripts/capture_frames.py`
- Test: `tests/scripts/test_capture_frames.py`

- [ ] **Step 1: Escrever o teste**

```python
# tests/scripts/test_capture_frames.py
from __future__ import annotations
from scripts.terminal_template import TermLine


def test_capture_returns_bytes_list():
    from scripts.capture_frames import capture_frames
    frames_spec = [
        {"lines": [], "show_cursor": True, "delay_ms": 500},
        {"lines": [TermLine("hello", color=None)], "delay_ms": 800},
    ]
    frames = capture_frames(frames_spec)
    assert len(frames) == 2
    for png_bytes, delay_ms in frames:
        assert isinstance(png_bytes, bytes)
        assert len(png_bytes) > 1000
        assert png_bytes[:4] == b'\x89PNG'
        assert delay_ms > 0


def test_capture_correct_delays():
    from scripts.capture_frames import capture_frames
    frames_spec = [
        {"lines": [], "delay_ms": 300},
        {"lines": [], "delay_ms": 1500},
    ]
    frames = capture_frames(frames_spec)
    assert frames[0][1] == 300
    assert frames[1][1] == 1500


def test_capture_partial_prompt_frame():
    """Frame com partial_prompt deve capturar sem erro."""
    from scripts.capture_frames import capture_frames
    frames_spec = [
        {"lines": [], "partial_prompt": "clawlite", "show_cursor": True, "delay_ms": 80},
    ]
    frames = capture_frames(frames_spec)
    assert frames[0][0][:4] == b'\x89PNG'
```

- [ ] **Step 2: Rodar para confirmar falha**

```bash
cd /root/build/ClawLite && python3 -m pytest tests/scripts/test_capture_frames.py -v 2>&1 | tail -10
```
Expected: `ModuleNotFoundError`

- [ ] **Step 3: Criar `scripts/capture_frames.py`**

```python
# scripts/capture_frames.py
"""Captura frames PNG do template HTML usando Playwright (chromium headless)."""
from __future__ import annotations
from typing import Any

from scripts.terminal_template import build_html


def capture_frames(
    frames_spec: list[dict[str, Any]],
    *,
    width: int = 720,
    height: int = 400,
) -> list[tuple[bytes, int]]:
    """
    Recebe lista de especificações de frame, retorna lista de (png_bytes, delay_ms).

    Chaves reconhecidas em cada spec dict:
      lines: list[TermLine]         (padrão [])
      show_prompt: bool             (padrão True)
      partial_prompt: str | None    (padrão None — ativa typing animation)
      show_cursor: bool             (padrão False)
      spinner: str | None           (padrão None)
      delay_ms: int                 (obrigatório)
    """
    from playwright.sync_api import sync_playwright

    results: list[tuple[bytes, int]] = []

    with sync_playwright() as pw:
        browser = pw.chromium.launch(headless=True)
        page = browser.new_page(viewport={"width": width, "height": height})

        for spec in frames_spec:
            html = build_html(
                lines=spec.get("lines", []),
                width=width,
                height=height,
                show_prompt=spec.get("show_prompt", True),
                partial_prompt=spec.get("partial_prompt"),
                show_cursor=spec.get("show_cursor", False),
                spinner=spec.get("spinner"),
            )
            page.set_content(html, wait_until="domcontentloaded")
            png_bytes = page.screenshot(type="png")
            results.append((png_bytes, int(spec["delay_ms"])))

        browser.close()

    return results
```

- [ ] **Step 4: Rodar testes (~15s)**

```bash
cd /root/build/ClawLite && python3 -m pytest tests/scripts/test_capture_frames.py -v -s 2>&1 | tail -15
```
Expected: `3 passed`

- [ ] **Step 5: Commit**

```bash
cd /root/build/ClawLite
git add scripts/capture_frames.py tests/scripts/test_capture_frames.py
git commit -m "feat(scripts): add Playwright frame capture (supports partial_prompt for typing animation)"
```

---

## Chunk 3: Montagem do GIF

### Task 3: Montar GIF animado com Pillow

**Files:**
- Create: `scripts/assemble_gif.py`
- Test: `tests/scripts/test_assemble_gif.py`

- [ ] **Step 1: Escrever o teste**

```python
# tests/scripts/test_assemble_gif.py
from __future__ import annotations
import io
from PIL import Image


def _make_fake_png(color: tuple, width: int = 720, height: int = 400) -> bytes:
    img = Image.new("RGB", (width, height), color)
    buf = io.BytesIO()
    img.save(buf, format="PNG")
    return buf.getvalue()


def test_assemble_creates_gif(tmp_path):
    from scripts.assemble_gif import assemble_gif
    frames = [
        (_make_fake_png((30, 30, 46)), 500),
        (_make_fake_png((137, 180, 250)), 800),
        (_make_fake_png((166, 227, 161)), 300),
    ]
    output = tmp_path / "test.gif"
    assemble_gif(frames, output_path=str(output))
    assert output.exists()
    assert output.stat().st_size > 1000


def test_assemble_gif_loops_forever(tmp_path):
    from scripts.assemble_gif import assemble_gif
    frames = [(_make_fake_png((30, 30, 46)), 500), (_make_fake_png((50, 50, 70)), 500)]
    output = tmp_path / "loop.gif"
    assemble_gif(frames, output_path=str(output))
    img = Image.open(str(output))
    # Pillow popula info["loop"]=0 ao reabrir GIF salvo com loop=0
    assert img.info.get("loop") == 0, f"esperado loop=0, got: {img.info.get('loop')!r}"


def test_assemble_gif_frame_count(tmp_path):
    from scripts.assemble_gif import assemble_gif
    frames = [(_make_fake_png((i * 20, i * 20, i * 20)), 200) for i in range(5)]
    output = tmp_path / "frames.gif"
    assemble_gif(frames, output_path=str(output))
    img = Image.open(str(output))
    count = 0
    try:
        while True:
            count += 1
            img.seek(img.tell() + 1)
    except EOFError:
        pass
    assert count == 5
```

- [ ] **Step 2: Rodar para confirmar falha**

```bash
cd /root/build/ClawLite && python3 -m pytest tests/scripts/test_assemble_gif.py -v 2>&1 | tail -10
```
Expected: `ModuleNotFoundError`

- [ ] **Step 3: Criar `scripts/assemble_gif.py`**

```python
# scripts/assemble_gif.py
"""Monta frames PNG em GIF animado usando Pillow."""
from __future__ import annotations
import io
from pathlib import Path
from PIL import Image


def assemble_gif(
    frames: list[tuple[bytes, int]],
    *,
    output_path: str,
    optimize: bool = True,
) -> None:
    """
    Salva GIF animado com loop infinito.

    Args:
        frames: lista de (png_bytes, delay_ms)
        output_path: caminho do .gif de saída
        optimize: reduz paleta (menor arquivo, sem perda perceptível)
    """
    if not frames:
        raise ValueError("frames list is empty")

    pil_frames: list[Image.Image] = []
    durations: list[int] = []

    for png_bytes, delay_ms in frames:
        img = Image.open(io.BytesIO(png_bytes)).convert("RGB")
        pil_frames.append(img.convert("P", palette=Image.ADAPTIVE, colors=256))
        durations.append(max(20, delay_ms))

    output = Path(output_path)
    output.parent.mkdir(parents=True, exist_ok=True)

    pil_frames[0].save(
        str(output),
        format="GIF",
        save_all=True,
        append_images=pil_frames[1:],
        optimize=optimize,
        loop=0,          # 0 = loop infinito
        duration=durations,
    )
```

- [ ] **Step 4: Rodar testes**

```bash
cd /root/build/ClawLite && python3 -m pytest tests/scripts/test_assemble_gif.py -v 2>&1 | tail -15
```
Expected: `3 passed`

- [ ] **Step 5: Commit**

```bash
cd /root/build/ClawLite
git add scripts/assemble_gif.py tests/scripts/test_assemble_gif.py
git commit -m "feat(scripts): add Pillow GIF assembly (loop=0 infinite, per-frame delays)"
```

---

## Chunk 4: Registrar marker `slow` no pytest

### Task 4: Adicionar marker antes dos testes de integração

**Files:**
- Modify: `pyproject.toml`

- [ ] **Step 1: Ver configuração atual**

```bash
cd /root/build/ClawLite && grep -A 15 "\[tool.pytest" pyproject.toml
```

- [ ] **Step 2: Adicionar marker `slow` em `[tool.pytest.ini_options]`**

Em `pyproject.toml`, dentro da seção `[tool.pytest.ini_options]`, adicionar a chave `markers`:

```toml
markers = [
    "slow: testes de integração lentos (pule com -m 'not slow')",
]
```

- [ ] **Step 3: Confirmar sem warnings**

```bash
cd /root/build/ClawLite && python3 -m pytest tests/scripts/ -v --tb=short 2>&1 | tail -15
```
Expected: testes existentes passam, sem `PytestUnknownMarkWarning`

- [ ] **Step 4: Commit**

```bash
cd /root/build/ClawLite
git add pyproject.toml
git commit -m "test: register slow marker in pytest for integration tests"
```

---

## Chunk 5: Script principal e integração

### Task 5: Orquestrar em `make_demo_gif.py`

**Files:**
- Create: `scripts/make_demo_gif.py`
- Test: `tests/scripts/test_make_demo_gif.py`

- [ ] **Step 1: Escrever o teste**

```python
# tests/scripts/test_make_demo_gif.py
from __future__ import annotations
import pytest


def test_build_frames_spec_structure():
    """build_frames_spec() deve retornar >= 15 frames com campos obrigatórios."""
    from scripts.make_demo_gif import build_frames_spec
    spec = build_frames_spec()
    assert isinstance(spec, list)
    assert len(spec) == 16
    for item in spec:
        assert "lines" in item
        assert "delay_ms" in item
        assert item["delay_ms"] > 0


def test_build_frames_spec_has_typing_frames():
    """Primeiros frames devem ter partial_prompt (efeito de digitação)."""
    from scripts.make_demo_gif import build_frames_spec
    spec = build_frames_spec()
    typing_frames = [f for f in spec if f.get("partial_prompt")]
    assert len(typing_frames) >= 5, "Esperado pelo menos 5 frames de digitação"


def test_build_frames_spec_has_final_pause():
    """Último frame deve ter delay >= 2000ms."""
    from scripts.make_demo_gif import build_frames_spec
    spec = build_frames_spec()
    assert spec[-1]["delay_ms"] >= 2000


@pytest.mark.slow
def test_make_demo_gif_generates_file(tmp_path):
    """Integração completa — gera GIF real via Playwright. ~25s."""
    from scripts.make_demo_gif import make_demo_gif
    output = tmp_path / "demo.gif"
    make_demo_gif(output_path=str(output))
    assert output.exists()
    assert output.stat().st_size > 50_000
```

- [ ] **Step 2: Rodar para confirmar falha (sem slow)**

```bash
cd /root/build/ClawLite && python3 -m pytest tests/scripts/test_make_demo_gif.py -v -k "not slow" 2>&1 | tail -10
```
Expected: `ModuleNotFoundError`

- [ ] **Step 3: Criar `scripts/make_demo_gif.py`**

```python
# scripts/make_demo_gif.py
"""Gera docs/demo.gif — demo animada do ClawLite para o README.

Uso:
    python3 scripts/make_demo_gif.py
    python3 scripts/make_demo_gif.py --output /caminho/custom.gif

16 frames: digitação → cursor piscando → spinner → resposta em streaming.
Sem API key — tudo scripted.
"""
from __future__ import annotations
import argparse
from pathlib import Path

from scripts.terminal_template import TermLine, _FULL_CMD, _FULL_RUN, _FULL_ARG
from scripts.capture_frames import capture_frames
from scripts.assemble_gif import assemble_gif

_BLUE  = "#89b4fa"
_GRAY  = "#6c7086"

_DEFAULT_OUTPUT = Path(__file__).parent.parent / "docs" / "demo.gif"

# Estados de digitação: trecho do comando sendo digitado
_TYPING_STATES = [
    "c",
    _FULL_CMD,
    _FULL_CMD + _FULL_RUN.rstrip(),
    _FULL_CMD + _FULL_RUN + '"o que você',
    _FULL_CMD + _FULL_RUN + _FULL_ARG,
]


def build_frames_spec() -> list[dict]:
    intro = TermLine("Posso ajudar com muita coisa! Aqui o resumo:")
    bullets = [
        TermLine("🧠 Memória  — lembro do que conversamos entre sessões", color=_BLUE),
        TermLine("🔍 Busca    — pesquiso na web em tempo real",           color=_BLUE),
        TermLine("💻 Código   — escrevo, reviso e executo scripts",       color=_BLUE),
        TermLine("📂 Arquivos — leio, crio e edito arquivos locais",      color=_BLUE),
        TermLine("📡 Canais   — respondo no Telegram e Discord",          color=_BLUE),
    ]
    footer = TermLine("Use clawlite skills list para ver tudo.", color=_GRAY)

    frames: list[dict] = []

    # Frames 1–5: digitação (80ms por frame)
    for state in _TYPING_STATES:
        frames.append({"lines": [], "partial_prompt": state, "show_cursor": True, "delay_ms": 80})

    # Frames 6–7: cursor piscando (prompt completo, 400ms cada)
    frames.append({"lines": [], "show_cursor": True,  "delay_ms": 400})
    frames.append({"lines": [], "show_cursor": False, "delay_ms": 400})

    # Frames 8–9: spinner (800ms cada)
    frames.append({"lines": [], "show_cursor": False, "spinner": "⠸ pensando...", "delay_ms": 800})
    frames.append({"lines": [], "show_cursor": False, "spinner": "⠴ pensando...", "delay_ms": 800})

    # Frame 10: intro da resposta
    frames.append({"lines": [intro], "delay_ms": 600})

    # Frames 11–15: bullets progressivos
    for i in range(len(bullets)):
        frames.append({"lines": [intro] + bullets[: i + 1], "delay_ms": 520})

    # Frame 16: footer + pausa final
    frames.append({"lines": [intro] + bullets + [TermLine(""), footer], "delay_ms": 3200})

    return frames


def make_demo_gif(*, output_path: str | None = None) -> None:
    out = str(output_path or _DEFAULT_OUTPUT)
    spec = build_frames_spec()
    print(f"⠸ Capturando {len(spec)} frames via Playwright...")
    frames = capture_frames(spec)
    print(f"⠸ Montando GIF em {out} ...")
    assemble_gif(frames, output_path=out)
    size_kb = Path(out).stat().st_size // 1024
    print(f"✓ {out} gerado ({size_kb} KB)")


if __name__ == "__main__":
    parser = argparse.ArgumentParser(description="Gera docs/demo.gif para o README")
    parser.add_argument("--output", default=None, help="Caminho de saída do GIF")
    args = parser.parse_args()
    make_demo_gif(output_path=args.output)
```

- [ ] **Step 4: Rodar testes unitários (sem slow)**

```bash
cd /root/build/ClawLite && python3 -m pytest tests/scripts/test_make_demo_gif.py -v -k "not slow" 2>&1 | tail -15
```
Expected: `3 passed`

- [ ] **Step 5: Rodar teste de integração completo (~25s)**

```bash
cd /root/build/ClawLite && python3 -m pytest tests/scripts/test_make_demo_gif.py::test_make_demo_gif_generates_file -v -s 2>&1 | tail -20
```
Expected: `1 passed` — GIF gerado em `tmp_path`

- [ ] **Step 6: Gerar o GIF real**

```bash
cd /root/build/ClawLite && python3 scripts/make_demo_gif.py
```
Expected:
```
⠸ Capturando 16 frames via Playwright...
⠸ Montando GIF em .../docs/demo.gif ...
✓ docs/demo.gif gerado (XXX KB)
```

- [ ] **Step 7: Verificar propriedades do GIF**

```bash
cd /root/build/ClawLite && python3 -c "
from PIL import Image
img = Image.open('docs/demo.gif')
print('size:', img.size)
print('loop:', img.info.get('loop'))
n = 0
try:
    while True: img.seek(img.tell()+1); n+=1
except EOFError: pass
print('frames:', n+1)
"
```
Expected: `size: (720, 400)`, `loop: 0`, `frames: 16`

- [ ] **Step 8: Commit**

```bash
cd /root/build/ClawLite
git add scripts/make_demo_gif.py tests/scripts/test_make_demo_gif.py docs/demo.gif
git commit -m "feat(scripts): add make_demo_gif.py — animated README demo via Playwright+Pillow

- 16 frames: typing animation → cursor blink → spinner → streaming bullets
- macOS-style terminal (Catppuccin Mocha, 720x400px, loop infinito)
- Runs standalone: python3 scripts/make_demo_gif.py"
```
